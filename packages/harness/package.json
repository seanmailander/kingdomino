{
  "name": "kingdomino-harness",
  "version": "0.0.1",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "tscheck": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.harness.json",
    "build": "tsc -p tsconfig.json",
    "harness:build": "tsc -p tsconfig.harness.json",
    "harness": "node dist-harness/harness.js",
    "harness:server": "node dist-harness/mcp-server.js"
  },
  "dependencies": {
    "kingdomino-engine": "*"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^4.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}

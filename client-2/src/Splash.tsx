import "react";

export function SplashComponent() {
  return (
    <>
      <button aria-label="Join lobby" onClick={() => {}}>
        Ready for a game with friends?
      </button>
      <button aria-label="Start solo" onClick={() => {}}>
        Ready for a game on your own?
      </button>
    </>
  );
}
